import "../Header/style.scss";

const Header = () => {
  return (
    <div>
      <header>
        <h1>Task Manager</h1>
        <button>Repository</button>
      </header>
    </div>
  );
};

export default Header;
